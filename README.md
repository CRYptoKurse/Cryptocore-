📋 Краткое описание
CryptoCore - инструмент для шифрования и дешифрования данных с использованием AES-128 в различных режимах работы. В третьем спринте реализован криптографически стойкий генератор случайных чисел (CSPRNG) для безопасной генерации ключей и векторов инициализации.

🚀 Инструкция по использованию
Шифрование с автоматической генерацией ключа:
bash
cryptocore --algorithm aes --mode ctr --encrypt --input plaintext.txt --output ciphertext.bin
Программа сгенерирует и отобразит случайный ключ в терминале.

Шифрование с указанием ключа:
bash
cryptocore --algorithm aes --mode ctr --encrypt --key 1a2b3c4d5e6f7890fedcba9876543210 --input plaintext.txt --output ciphertext.bin
Дешифрование (ключ обязателен):
bash
cryptocore --algorithm aes --mode ctr --decrypt --key 1a2b3c4d5e6f7890fedcba9876543210 --input ciphertext.bin --output decrypted.txt
✅ Выполненные требования
Основные требования спринта 3:
CLI интерфейс:

Ключ стал опциональным для шифрования

Автоматическая генерация ключа при его отсутствии

Вывод сгенерированного ключа в терминал

Ключ обязателен для дешифрования

CSPRNG модуль:

Создан выделенный модуль csprng.py

Используется криптографически стойкий os.urandom()

Интегрирован для генерации ключей и IV

Управление ключами и IV:

Автоматическая генерация 16-байтных ключей AES-128

Генерация IV через CSPRNG для всех режимов (кроме ECB)

Ключ не записывается в выходной файл

🔧 Как работает
При шифровании без ключа:

Вызывается generate_random_bytes(16) из csprng.py

Генерируется 16-байтный случайный ключ

Ключ отображается в терминале в hex-формате

Используется для шифрования данных

Генерация IV:

Для режимов CBC, CFB, OFB, CTR генерируется случайный IV

IV добавляется в начало зашифрованного файла

Обработка данных:

Реализованы все режимы AES-128: ECB, CBC, CFB, OFB, CTR

Поддержка PKCS#7 padding для ECB и CBC

Потоковые режимы работают без дополнения

📊 Тестирование и верификация
Статистические тесты NIST
Проведено тестирование генератора случайных чисел с использованием NIST Statistical Test Suite (STS). Все 15 тестов пройдены успешно, что подтверждает криптографическую стойкость реализации.

Ключевые результаты:

Все тесты показали p-value ≥ 0.01

Отсутствие статистических аномалий

Высокая энтропия сгенерированных данных


Файл с результатами тестирования
Подробные результаты тестирования NIST STS находятся в файле: finalAnalysisReport.txt

Совместимость: Сохранена полная совместимость с предыдущими спринтами



